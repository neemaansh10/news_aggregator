# ---------------------------------------------------------------------------
# News Aggregator - runtime dependencies
# Python 3.9+  (tested on 3.9 and 3.11)
# ---------------------------------------------------------------------------

# --- Web / API layer -------------------------------------------------------
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0

# --- Persistence -----------------------------------------------------------
SQLAlchemy[asyncio]>=2.0,<2.1
aiosqlite>=0.19            # default local database driver
# asyncpg>=0.29            # uncomment for PostgreSQL in staging/production

# --- Ingestion -------------------------------------------------------------
httpx>=0.26,<1.0
feedparser>=6.0

# --- Optional, enabled by configuration ------------------------------------
# redis>=5.0               # shared cache + distributed DF counters
# aiokafka>=0.10           # replaces the in-memory event bus
# prometheus-client>=0.19  # exposes /metrics in Prometheus format
